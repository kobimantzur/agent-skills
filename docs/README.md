# docs

Drop `site-risk-check.gif` here — a short screen recording of the skill running
(ask your agent "check if my site is legally covered — yoursite.com", record the
reply). The main README references it as `docs/site-risk-check.gif`.

Record with QuickTime (Cmd-Shift-5) or any screen recorder, export to GIF.
